identity:
  id: "aria.UriPath"
  purpose: "Branded type for URI path strings"
  domain: "aria"
  subdomain: "type"
  verb: null
  entity: "UriPath"

layer: L0_PRIMITIVE

contract:
  input: "string"
  output:
    success: "UriPath"
    failure: null
  side_effects: NONE
  idempotent: true
  deterministic: true

dependencies: []

connections: []

context_budget:
  to_use: 50
  to_modify: 100
  to_extend: 200
  to_replace: 80

test_contract:
  scenarios:
    - "creates UriPath via from()"
    - "UriPath equal instances are equal"
    - "UriPath different instances are not equal"

stability: EXPERIMENTAL

lifecycle:
  phase: DRAFT
